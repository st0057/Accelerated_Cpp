#include <string>
#include <vector>
#include "split.h"

using std::string;      using std::vector;

int main() {
  string s1 = "The quick brown fox";
  string s2 = "jumped over the fence";

  s1_vec = split(s1);
  s2_vec = split(s2);

  s1_vec.
}