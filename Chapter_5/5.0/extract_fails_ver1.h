#ifndef GUARD_extract_fails_ver1
#define GUARD_extract_fails_ver1

#include <vector>
#include "Student_info.h"

std::vector<Student_info> extract_fails(std::vector<Student_info>&)
bool fgrade(const Student_info&)

#endif