#ifndef GUARD_extract_fails_ver3
#define GUARD_extract_fails_ver3

#include <list>
#include "Student_info.h"

std::list<Student_info> extract_fails(std::list<Student_info>&);
bool fgrade(const Student_info&);

#endif