#include <iostream>
#include <string>
#include <vector>

bool is_palindrome(const std::string&);
std::istream& read(std::istream&, std::vector<std::string>&);
std::vector<std::string> find_palindrome(std::vector<std::string>&);